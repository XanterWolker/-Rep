using System;

namespace Учеба
{
    class Program
    {
        static void Main(string[] args)
        {
            /* int num = 20;
            num =+ 5;
             Console.WriteLine( "Переменная:" + num);
            */


            /*
            double num = 1.4234;
            double cub = Math.Pow(num, 3);
            cub = Math.Round(cub);

            Console.WriteLine(cub);
            */

            char[] arr = new char[] {'J', 'a', 'v', 'a', '!' };

            Console.WriteLine("Элемент: " + arr[2]);
        }
    }
}
